//
function add(a, b) {
    return a + b;
}

//if we do not return anything, the function will return undefined

//console.log(10); //undefined
//console.log() is also a function defined in js, which is used to print the output to the console and it returns undefined.